name: Deploy to Netlify

on:
  push:
    branches: [main]
    paths:
      - 'web/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify deploy
        run: curl -s -X POST "https://api.netlify.com/build_hooks/6a19555e661d52388a56903f"
